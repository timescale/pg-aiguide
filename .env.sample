# Database
PGHOST=db
PGPORT=5432
PGDATABASE=tsdb
PGUSER=tsdbadmin
PGPASSWORD=password
DB_SCHEMA=docs

# OpenAI Embedding API Key
OPENAI_API_KEY=sk-

# Optional: Custom OpenAI-compatible API endpoint (e.g., local ollama, third-party services)
# OPENAI_BASE_URL=http://localhost:11434/v1

# Optional: Custom embedding model name (default: text-embedding-3-small)
# IMPORTANT: All data in the database must use the SAME embedding model!
# Mixing embeddings from different models will produce meaningless search results.
# If you change the model, you must re-scrape all documentation.
# EMBEDDING_MODEL=text-embedding-3-small

# Optional: Embedding dimensions (default: 1536, must match database schema)
# Note: Some models perform best at their native dimensions (e.g., Qwen3-Embedding-8B at 4096),
# but 1536 is used for compatibility with OpenAI and existing database schema.
# EMBEDDING_DIMENSIONS=1536
